/_build/
/cover/
.DS_Store
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/
facturx-*.tar

# Local, private fixtures (real invoices) — never commit
/test/fixtures/local/

# python dev venv
/.venv-dev/

# Local working notes (roadmap, scratch) — never committed
*.local.md
